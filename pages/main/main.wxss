/* pages/main/main.wxss */
.container {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: space-between;
  height: 100vh; /* 视口高度 */
}

.top-image {
  width: 100%;
  /* height: 900px; */
  max-width: 300px; /* 根据需要调整最大宽度 */
}

button {
  margin-bottom: 50px; /* 根据需要调整按钮与底部的距离 */
}